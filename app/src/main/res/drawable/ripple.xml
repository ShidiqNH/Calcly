<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/iconColor"> <!-- Set your ripple color here -->

    <item android:id="@android:id/mask">
        <shape android:shape="oval"> <!-- Circular mask -->
            <solid android:color="@color/iconColor" /> <!-- Background color -->
        </shape>
    </item>
</ripple>
